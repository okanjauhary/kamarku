const initialState = {
    fetching: false,
    fetched: false,
    error: null,
    rooms : ["OK"],
    room: {}
}

const roomData = (state = initialState, action) => {
    return state
}

export default roomData